{
  "name": "@deux/models",
  "version": "0.0.1",
  "description": "model for deux tools",
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "npm run formatter && tsc -p tsconfig.json",
    "codegen": "node dist/tools/main",
    "formatter": "prettier --write \"src/**/*.ts\""
  },
  "keywords": [
    "typescript"
  ],
  "author": "Fumio SAGAWA",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^13.13.5",
    "prettier": "^2.0.5",
    "rimraf": "^3.0.2",
    "tslib": "^1.11.2",
    "typescript": "^3.8.3"
  }
}
